class DeviceService {
  constructor(protocol, config) { // eslint-disable-line no-unused-vars
    switch (protocol) {
      case 'DBUS':
        // init service
        break;
      default:
        break;
    }
  }

  execute() { // eslint-disable-line class-methods-use-this
  }

  listDevices() { // eslint-disable-line class-methods-use-this
  }

  getDevice(id) { // eslint-disable-line no-unused-vars, class-methods-use-this
  }
}

export default DeviceService;
